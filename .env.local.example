# Environment variables para desarrollo local

# Backend API URL (solo para desarrollo local)
# En producción, Vercel usa el proxy configurado en vercel.json
VITE_API_BASE_URL=http://localhost:8000

# Modo Mock: usar datos de prueba en lugar del backend real
# Cambia a 'true' para habilitar el modo mock
VITE_USE_MOCK_DATA=true

# Nota: Para usar datos reales del backend, cambia VITE_USE_MOCK_DATA a 'false'
# o simplemente comenta/elimina esa línea
